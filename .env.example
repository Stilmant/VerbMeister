# Server Configuration
PORT=3000
NODE_ENV=development

# Database Paths (LowDB split public/private)
# Public DB: verbs (tracked in git)
DB_PUBLIC_PATH=./server/data/verbs.public.json
# Private DB: users, progress (NOT tracked in git)
DB_PRIVATE_PATH=./server/data/users.private.json

# JWT Secret (change in production!)
JWT_SECRET=your-super-secret-key-change-me-in-production

# OpenAI API (optionnel, pour phase ultérieure)
# OPENAI_API_KEY=sk-...
